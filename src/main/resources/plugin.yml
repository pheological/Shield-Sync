name: ShieldSync
version: '${version}'
main: pheological.shieldSync.ShieldSync
api-version: '1.21'
depend: [packetevents]
commands:
  shieldsync:
    description: ShieldSync debug and control command
    usage: /<command> <enable|disable|status> | /<command> debug [player]
